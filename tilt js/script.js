/*
${'.class'}
${'#id'}
${'tag'}


*/

$('.box2').tilt({
    glare: true,
    maxGlare: 10,
    reset: true,
    scale: 1.2, // for zoom use more than 1 and for zoom-out use less than 1
})

$('.box3').tilt({
    glare: true,
    maxGlare: 2,
});